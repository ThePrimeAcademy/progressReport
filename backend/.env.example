PORT=5000

# Email (Resend) — required to send report cards from /api/report/email
# Sign up at https://resend.com, create an API key, then verify a sending domain.
# EMAIL_FROM must use a domain you've verified in Resend.
RESEND_API_KEY=
EMAIL_FROM="Prime Academy <reports@example.com>"
